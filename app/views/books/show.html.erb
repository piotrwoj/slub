<%= flash_fields %>

<p>
  <strong>Tytuł:</strong>
  <%= @book.title %>
</p>

<% if false %>
<p>
  <strong>Opis:</strong>
  <%= @book.description %>
</p>
<% end %>

<p>
  <strong>Autor:</strong>
  <%= @book.author %>
</p>

<p>
  <strong>Zarezerwowana:</strong>
  <%= @book.reserved? ? 'TAK' : 'NIE' %>
</p>

<% if admin? %>
<p>
  <strong>Ukryta notatka:</strong>
  <%= @book.hidden_note %>
</p>
<% end %>

<% if admin? %>
<%= link_to 'Edytuj', edit_book_path(@book) %> |
<%= link_to 'Usuń', book_path(@book), method: :delete, data: { confirm: 'Na pewno usunąć?' } %> |
<% end %>
<%= link_to 'Wróć', books_path %>

<% if admin? %>
  <br><br>
  <%= render 'reservations' %>
<% end %>
